# 通用编码约束
- 所有代码注释必须使用中文。
- 变量名和函数名使用英文，采用驼峰命名法（camelCase）。
- 除非我明确要求，否则不要生成解释文档，直接给出代码。

## 代码质量
- 单个文件不超过500行，单个函数不超过50行。
- 所有外部输入必须验证，所有错误必须处理（不静默）。

## 输出规范
- 给出代码时，附带必要的 import 和依赖说明。
- 提供多个方案时必须标注推荐方案和原因。
- 不确定时明确说"需要更多信息"，不编造API。

## 测试要求
- 核心业务逻辑必须有单元测试（覆盖率≥70%）。
- Bug修复必须附带回归测试。